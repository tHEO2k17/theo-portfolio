# Archived components

Legacy visual components removed from the live site. Reference only — do not import into active routes.

## `legacy/`

Pre-editorial portfolio template: bold CTAs, cards, gradients, Lucide icons.

Includes `articles-explorer.tsx` (superseded by `WritingIndex`) and `page-transition.tsx` (removed for stillness pass).

## `legacy/media/`

Atmospheric drift and floating background layers (removed for stillness pass).
